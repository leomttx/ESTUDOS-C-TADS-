#include <stdio.h>

int main() {
    int x, n, cont, l;
    scanf("%d %d", &x, &n);
    cont = 0;
    //while (cont < n)
    //{
        scanf("%d", &l);
        if (l % 2 == 0){
            cont++
        }
        // cont++;
    //}
    printf("%d", cont);
    return 0;
}